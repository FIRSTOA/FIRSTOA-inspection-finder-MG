{
  "name": "first-data-mg",
  "version": "1.0.0",
  "private": true,
  "description": "First 통합DB - Google Apps Script (clasp 배포)",
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  },
  "scripts": {
    "login": "clasp login",
    "push": "clasp push",
    "pull": "clasp pull",
    "open": "clasp open",
    "deploy": "clasp push && clasp deploy"
  }
}
